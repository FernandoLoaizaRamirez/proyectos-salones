# ------------------------------------------------------------------
# Marca (white-label). Cámbialo por el nombre del salón/cliente.
# ------------------------------------------------------------------
NEXT_PUBLIC_BRAND_NAME="Salón de Eventos"
# NEXT_PUBLIC_BRAND_LOGO_URL=

# ------------------------------------------------------------------
# Modo de venta / licencia (RESERVADO: aún no está activo).
# ------------------------------------------------------------------
APP_MODE=OWNED

# ------------------------------------------------------------------
# Integración opcional entre apps (degradación elegante).
# Si se define, la app usa el evento compartido; si no, funciona
# con sus propios datos locales.
# ------------------------------------------------------------------
# NEXT_PUBLIC_SHARED_BACKEND_URL=
