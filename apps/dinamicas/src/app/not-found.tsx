/**
 * Dirección que no existe. En un evento casi siempre es un enlace copiado a
 * medias o un QR mal escaneado, así que el texto va por ahí.
 */
import { PantallaNoEncontrada } from "@salones/ui";

export default function NoEncontrado() {
  return <PantallaNoEncontrada />;
}
