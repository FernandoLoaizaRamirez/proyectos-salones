import config from "@salones/config/eslint/base";

/**
 * Solo cubre lo que vive en la raíz del repo (las pruebas de integración y la
 * configuración de vitest). Cada app y cada paquete traen el suyo.
 */
export default config;
